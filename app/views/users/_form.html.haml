= simple_form_for @user do |f|
  = f.input :first_name
  = f.input :last_name
  - unless @user.persisted?
    = f.input :email
    = f.input :password
    = f.input :password_confirmation, label: 'Confirmation'
  = f.button :submit, 'Register', class: 'button tiny'
